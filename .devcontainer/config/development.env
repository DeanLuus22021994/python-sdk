# MCP Python SDK Environment Configuration
# This file consolidates all environment variables and references the global configuration
# Use this as the primary environment file for development

# Load global configuration (this will be loaded first)
# All GLOBAL_ prefixed variables are defined in docker-globals.env

# === PROJECT CONFIGURATION ===
WORKSPACE_FOLDER=/workspaces/python-sdk
DEV_CONTAINER_NAME=mcp-python-sdk-dev
REMOTE_USER=mcp

# === SERVICE PORTS (Local Development) ===
MCP_SERVER_PORT=5000
FASTAPI_PORT=8000
UVICORN_PORT=8080
JUPYTER_PORT=8888

# === SWARM CONFIGURATION ===
SWARM_REPLICAS=1
SWARM_NODE_ROLE=manager

# === DEVELOPMENT FLAGS ===
DEBUG_MODE=true
DEVELOPMENT_MODE=true
HOT_RELOAD_ENABLED=true

# === FEATURE FLAGS ===
ENABLE_PERFORMANCE_MONITORING=true
ENABLE_GPU_ACCELERATION=true
ENABLE_DISTRIBUTED_COMPUTING=true
PROMETHEUS_ENABLED=true
GRAFANA_ENABLED=false
JAEGER_ENABLED=false

# === BUILD CONFIGURATION ===
BUILD_STAGE=development
BUILD_TARGET=runtime
BUILD_PLATFORM=linux/amd64

# === CACHE CONFIGURATION (Overrides for development) ===
# These can override global cache settings for local development
PYTHON_CACHE_SIZE=8G
PIP_CACHE_SIZE=4G
NODE_MODULES_CACHE_SIZE=2G

# === DATABASE CONFIGURATION (Development Overrides) ===
# Use these for local development, production uses GLOBAL_ prefixed variables
POSTGRES_HOST=localhost
REDIS_HOST=localhost

# === LOGGING CONFIGURATION ===
LOG_LEVEL=DEBUG
LOG_FORMAT=detailed
LOG_TO_FILE=true
LOG_ROTATION=true

# === PERFORMANCE TUNING (Development) ===
PYTHON_GIL_DISABLED=1
PYTHON_MALLOC_STATS=1
PYTHONDONTWRITEBYTECODE=1

# === SECURITY (Development Mode) ===
# These are relaxed for development - production uses GLOBAL_ settings
CORS_ALLOW_ALL=true
CSRF_PROTECTION=false
SSL_VERIFICATION=false

# Note: This file should be used in conjunction with docker-globals.env
# Global configuration takes precedence for infrastructure settings
# This file provides development-specific overrides and additional configuration
